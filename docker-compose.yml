version: '3.1'
services:
  frontendnodeapp:
    build:
      context: ./frontend
      dockerfile: /Dockerfile
    image: test-frontend-docker-build
    container_name: frontendnextapp
    ports:
      - "3000:3000"
      
  backendnodeapp:
    build:
      context: ./backend
      dockerfile: /Dockerfile
    image: test-frontend-docker-build
    container_name: backendnodeapp
    ports:
      - "80:80"
